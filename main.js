import { ready } from './node_modules/smart-widget-handler/dist/index.js';

// Wait until DOM is fully loaded
window.addEventListener('DOMContentLoaded', () => {
  // Do any setup if needed

  // Then call ready
  ready();
});
